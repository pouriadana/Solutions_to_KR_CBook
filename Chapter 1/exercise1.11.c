/*
Input that contains only numbers. It counts each sequence of digits 
seperated by whitespace as a word. I do not think a sequence of digits
is considered a word.

Nonesense strings of punctuation with the length L where (L>=1)
It counts each string of punctuation as a word, when it's not
considered a word in English. 

This program successfully handles an empty text file.
*/